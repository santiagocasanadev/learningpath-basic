spring:
  application:
    name: learningpath-basic
  datasource:
    url: jdbc:mysql://localhost:3306/student_record_db?useSSL=false&serverTimezone=UTC
    username: root
    password: 654321
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080